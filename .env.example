# Frontend Environment Variables

# For separate backend deployment on Render:
VITE_API_URL=https://project-management-backend-in20.onrender.com/api

# For monolithic deployment (frontend + backend together), comment out the line above
# and the app will automatically use relative paths (/api)
